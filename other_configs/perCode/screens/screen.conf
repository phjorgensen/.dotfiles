mode "Screen settings: [l]aptop, [h]ome" {
  bindsym l exec --no-startup-id ~/.config/screens/laptop.sh; mode "default";
  bindsym h exec --no-startup-id ~/.config/screens/home.sh; mode "default";

  # back to normal: Enter or Escape
  bindsym Return mode "default"
  bindsym Escape mode "default"
}

bindsym $mod+shift+b mode "Screen settings: [l]aptop, [h]ome"
